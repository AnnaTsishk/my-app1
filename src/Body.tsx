type HeaderType ={
    title:string
}
type BodyType ={
    title:string
}

export const Header = (props: BodyType)=>{
    return (<div>
            Header

        </div>

    )

    export const Body = (props: HeaderType)=>{
        return (<div>
                Header
            </div>

        )

}